## Legacy Version

The original implementation of this project has been preserved in the `legacy` branch.

That version represents the initial solution created before the Java 17 modernization and refactoring process.

You can access it here:

```bash
git checkout legacy-java-8
```

The legacy branch is kept for:
- historical reference
- compatibility testing
- comparison between implementations
```
